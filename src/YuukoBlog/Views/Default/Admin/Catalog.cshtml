@model IEnumerable<Catalog>
@{
    ViewBag.Title = "Admin Panel";
}
@if (Html.IsRaw())
{
    @(new HtmlString("<div>"))
    <div id="raw_info">
        <div class="role-title">@ViewBag.Title</div>
        <div class="role-body-class">home</div>
        <div class="role-category">home</div>
        <div class="role-head-title">
            <h1>@ViewBag.Title</h1>
        </div>
    </div>
}
<div class="cont-wrap">
    <div class="grid_9">
        <div class="post">
            <div class="post-body">
                <p>
                    <a yuuko-static href="javascript:$('#frmNewCatalog').submit();">New Catalog</a>
                </p>
                <form asp-action="CatalogDelete" asp-controller="Admin" method="post" id="frmDeleteCatalog"></form>
                <form asp-action="CatalogEdit" asp-controller="Admin" method="post" id="frmEditCatalog"></form>
                <form asp-action="CatalogNew" asp-controller="Admin" method="post" id="frmNewCatalog"></form>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>Sub Title</th>
                            <th>Order</th>
                            <th>Control</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var x in Model)
                        { 
                            <tr data-catalog="@x.Url">
                                <td class="display d-title-zh">@x.Title</td>
                                <td class="display d-title">@x.Url</td>
                                <td class="display d-order">@x.Priority</td>
                                <td class="editing"><input class="title-zh" type="text" value="@x.Title" /></td>
                                <td class="editing"><input class="title" type="text" value="@x.Url" /></td>
                                <td class="editing"><input class="order" type="text" value="@x.Priority" /></td>
                                <td>
                                    <a class="display" href="javascript:editCatalog('@x.Url');">Edit</a>
                                    <a class="display" href="javascript:deleteCatalog('@x.Url');">Remove</a>
                                    <a class="editing" href="javascript:saveCatalog('@x.Url');">Save</a>
                                    <a class="editing" href="javascript:cancelEditCatalog();">Cancel</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    @Html.Partial("Sidebar")
</div>
@if (Html.IsRaw())
{
    @(new HtmlString("</div>"))
}